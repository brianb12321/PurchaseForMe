akka {
    stdout-loglevel = DEBUG
    loglevel = DEBUG
    log-config-on-start = on
    remote {
        dot-netty.tcp {
            port = 8081
            hostname = localhost
        }
    }
    cluster {
        seed-nodes = ["akka.tcp://purchaseForMe@localhost:8081"]
        roles = ["Pipeline", "Task"]
        role.["Pipeline"].min-nr-of-members = 1
        role.["Task"].min-nr-of-members = 1
    }
    actor {
        provider = cluster
        debug {
            receive = on
            autoreceive = on
            lifecycle = on
            event-stream = on
            unhandled = on
        }
    }
}